[project]
name = "mqtt-receiver"
version = "0.1.0"
description = "MQTT receiver for observing all topics"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["paho-mqtt>=2.1.0"]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D", "T201", "BLE001"]
